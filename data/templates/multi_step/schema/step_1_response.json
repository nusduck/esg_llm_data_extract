{
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "code": {"type": "string", "pattern": "^[A-Za-z0-9_]{1,20}$"},
            "item": {"type": "string", "pattern": "^[A-Za-z0-9 ]{1,50}$"}
        },
        "required": ["code", "item"]
    }
}